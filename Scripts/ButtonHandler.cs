using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace GuessWho
{
    public class ButtonHandler : MonoBehaviour
    {
        /*
        public void killEnemy(){
            Debug.Log("Clicked");
                if(AI.Contains(target)){
                    Debug.Log("Killed");
                    int index = AI.IndexOf(target);
                    Destroy(AI[index]);
                    AI.Remove(target);
            }
        }
        */
    }
}
